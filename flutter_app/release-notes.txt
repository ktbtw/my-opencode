- 设备详情页（分页布局）改为双栏仪表盘：左栏是设备身份与实时运行状态，右栏是磁盘占用与配置入口，整页收进一个面板，不再由多张卡片拼接
- 配置入口改为列表行，视觉上更安静，窄栏里不再堆叠方块控件
- 宽屏两栏并排、窄屏回落单列，阅读顺序在两种宽度下保持一致
- 修复模型上下文窗口手填后仍显示旧值的问题，手填值不再被运行时上报值覆盖
- 修复 Web 端部分浏览器打开一直白屏的问题：引擎文件改为同源自带，不再依赖境外 CDN
- Web 端不再使用离线缓存，避免版本升级后拿到新旧混合的文件
