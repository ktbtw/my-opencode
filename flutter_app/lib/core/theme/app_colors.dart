import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 主背景 - 冷白蓝渐变底色
  static const Color backgroundStart = Color(0xFFEFF4FB);
  static const Color backgroundEnd = Color(0xFFF7FAFF);
  static const Color background = Color(0xFFF2F6FC);

  // 面板
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFFAFCFF);

  // 边框
  static const Color border = Color(0xFFD0DDEF);
  static const Color borderLight = Color(0xFFE8EFF8);

  // 主色 - 蓝色系
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryHover = Color(0xFF1D4ED8);
  static const Color primaryLight = Color(0xFFEFF6FF);
  static const Color primaryMuted = Color(0xFFBFD7FF);

  // 文字
  static const Color textPrimary = Color(0xFF0F1729);
  static const Color textSecondary = Color(0xFF4A5D7A);
  static const Color textMuted = Color(0xFF8FA3BF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // 状态色
  static const Color statusOnline = Color(0xFF16A34A);
  static const Color statusOnlineLight = Color(0xFFDCFCE7);
  static const Color statusSuccess = Color(0xFF16A34A);
  static const Color statusSuccessLight = Color(0xFFDCFCE7);
  static const Color statusProcessing = Color(0xFF2563EB);
  static const Color statusProcessingLight = Color(0xFFEFF6FF);
  static const Color statusWarning = Color(0xFFD97706);
  static const Color statusWarningLight = Color(0xFFFEF3C7);
  static const Color statusError = Color(0xFFDC2626);
  static const Color statusErrorLight = Color(0xFFFEE2E2);
  static const Color statusOffline = Color(0xFF94A3B8);
  static const Color statusOfflineLight = Color(0xFFF1F5F9);

  // 分割线
  static const Color divider = Color(0xFFE4ECF5);

  // 输入框
  static const Color inputBackground = Color(0xFFF8FAFD);
  static const Color inputFocused = Color(0xFF2563EB);
}
