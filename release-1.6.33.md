# Release 1.6.33

## Goal
Publish the mobile Agent rename dialog and chat history scroll fixes as app version `1.6.33+180`.

## Tasks
- [x] Update app version and release metadata -> Verify version/history files agree.
- [x] Run Flutter tests and release metadata validation -> Verify all checks pass.
- [x] Build the Windows installer and deploy Web -> Verify both outputs use `1.6.33`.
- [x] Build and upload Android, Windows, and macOS packages -> Verify all assets are available.
- [x] Inspect production APK metadata and package hashes -> Verify `1.6.33+180` and all checksums.

## Done When
- [x] Production reports app `1.6.33+180` and all three client downloads pass validation.
