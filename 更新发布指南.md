# Chat Codex 更新与发布指南

## 1. 发布原则

1. 按实际修改范围发布对应项目。
2. Flutter 是一个完整发布单元，发布时固定包含 Web、Android APK、Windows EXE、macOS DMG。
3. Flutter 与 OpenCode/CLI 使用独立版本号、发布说明、版本接口和发布命令。
4. 后端接口或事件协议发生变化时，后端与 Flutter 一起发布。
5. `frida-runtime/phantom-work/frida` 等无关改动不纳入本次发布。
6. 普通更新不使用 `./deploy.sh all`，避免额外发布 CLI、Launcher 和运行时。

## 2. 判断修改范围

```bash
git status --short
git diff --name-only
```

| 修改目录 | 发布项目 |
| --- | --- |
| `backend/` | 后端 |
| `flutter_app/` | Flutter Web、APK、EXE、DMG |
| `my-opencode2/packages/opencode/` | OpenCode/CLI |
| `launcher/` | Launcher |
| `admin_web/` | 管理端 Web |
| 仅测试、文档、部署脚本 | 通常不增加产品版本 |

API、SSE、事件格式或历史分页变化按“后端 + Flutter”处理。

## 3. 版本号和发布说明

### Flutter

修改 `flutter_app/pubspec.yaml` 和 `flutter_app/release-notes.txt`。版本格式为：

```yaml
version: 1.6.58+205
```

`1.6.58` 是用户可见版本号，`205` 是 Android build number，必须高于线上版本。
`release-notes.txt` 只写 Flutter Web、Android、Windows、macOS 和客户端交互内容。
发布脚本会生成 `/api/app/version`、`/api/app/releases` 的元数据并维护
`flutter_app/release-history.json`。

### OpenCode / CLI

只修改 `my-opencode2/packages/opencode/package.json` 和
`my-opencode2/opencode-release-notes.txt`。说明只写 OpenCode/CLI 内容，发布使用：

```bash
./deploy.sh --cli-only
```

OpenCode 的版本和说明不写入 Flutter 文件，Flutter 的版本和说明也不写入 CLI 文件。

### Launcher 和后端

Launcher 使用 `launcher/version.txt` 和独立发布流程。后端当前没有独立用户版本号，
以 `chat-codex-server` 二进制、Git commit 和发布日期作为发布标识。

## 4. 发布前测试

```bash
cd backend
go test ./...
go test -race ./internal/store ./internal/api ./internal/app
go vet ./...

cd ../flutter_app
flutter analyze --no-pub
flutter test --no-pub
```

涉及 MySQL 事件存储或迁移时，还要执行服务器 MySQL 集成测试。

## 5. 构建 Flutter 四端

Web、APK、DMG 在 macOS 本机构建：

```bash
./deploy.sh --frontend-only
./deploy.sh --mobile-app-only
```

Windows EXE 在 Windows 构建机生成。**固定复用同一个构建目录，不要每个版本新建目录**：
构建产物每份约 600 MB，按版本建目录会一直堆积（历史上曾累积到 30 个目录 / 14 GB）。
每次重建目录也能保证是比较干净的源码树，避免上一版残留的文件混进编译。

```bash
BUILD_DIR="C:/Users/17150/chat-codex-build"
FLUTTER_BIN='C:\Users\17150\Desktop\flutter\flutter_windows_3.38.5-stable\flutter\bin'
VERSION=1.6.82

# 1. 打包当前源码（排除构建产物与 macOS 的 ._* 垃圾文件）
COPYFILE_DISABLE=1 bsdtar --no-xattrs --no-mac-metadata -czf /tmp/flutter_app.tgz \
  --exclude 'flutter_app/build' --exclude 'flutter_app/.dart_tool' \
  --exclude '._*' --exclude '.DS_Store' flutter_app

# 2. 删掉旧目录后重新解压，保证源码树干净
ssh windows-lan-yuzhihao "cmd /c \"rmdir /s /q C:\\Users\\17150\\chat-codex-build\""
ssh windows-lan-yuzhihao "cmd /c \"mkdir C:\\Users\\17150\\chat-codex-build\""
scp /tmp/flutter_app.tgz windows-lan-yuzhihao:$BUILD_DIR/
ssh windows-lan-yuzhihao "cmd /c \"cd /d C:\\Users\\17150\\chat-codex-build && tar -xzf flutter_app.tgz\""

# 3. 构建。Flutter 不在 SSH 会话的 PATH 里，必须显式指定；
#    并显式指向国内镜像：走 pub.dev 直连会卡在 Resolving dependencies（实测 25 分钟没进展）。
#    注意用 set "VAR=value" 的写法，set VAR=value & 会把尾部空格带进变量值。
ssh windows-lan-yuzhihao "cmd /c \"set \"PUB_HOSTED_URL=https://pub.flutter-io.cn\" & set \"FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn\" & set \"PATH=$FLUTTER_BIN;%PATH%\" & cd /d C:\\Users\\17150\\chat-codex-build\\flutter_app & powershell -NoProfile -ExecutionPolicy Bypass -File windows\\build_installer.ps1 -BaseUrl https://www.xyapi.top/codex\""

# 4. 取回安装包
scp "windows-lan-yuzhihao:$BUILD_DIR/flutter_app/build/windows/installer/ChatCodex-$VERSION-windows-x64-setup.exe" \
  flutter_app/build/windows/installer/

# 5. 构建目录用完即删，安装包已在本机归档
ssh windows-lan-yuzhihao "cmd /c \"rmdir /s /q C:\\Users\\17150\\chat-codex-build\""
```

然后将安装包带回本机，并指定给移动端发布命令：

```bash
CHAT_CODEX_WINDOWS_INSTALLER="/path/to/ChatCodex-1.6.58-windows-x64-setup.exe" \
  ./deploy.sh --mobile-app-only
```

该命令会校验 APK、EXE、DMG 的版本和 SHA256，并一起上传。

## 6. 构建和发布后端

```bash
./deploy.sh --backend-only
```

脚本会构建 Linux amd64 二进制、备份旧版本、重启 `chat-codex-backend`、检查
`/readyz`，启动失败时自动回滚。

服务器默认地址为 `root@www.xyapi.top`，公网地址为
`https://www.xyapi.top/codex`，后端内部端口为 `19081`。

## 7. 完整 Flutter + 后端发布顺序

```bash
./deploy.sh --backend-only
./deploy.sh --frontend-only
CHAT_CODEX_WINDOWS_INSTALLER="/path/to/ChatCodex-1.6.58-windows-x64-setup.exe" \
  ./deploy.sh --mobile-app-only
```

三个步骤使用同一个 Flutter 版本号。OpenCode/CLI 不在本次范围时，不执行
`--cli-only`，也不修改 CLI 发布说明。

## 8. 发布后验证

```bash
curl --fail https://www.xyapi.top/codex/healthz
curl --fail https://www.xyapi.top/codex/readyz
curl https://www.xyapi.top/codex/api/app/version
```

确认 Web、APK、EXE、DMG 下载正常，版本号和 SHA256 正确，事件分页和 SSE 断线补齐
正常。CLI 发布时单独检查 `/api/cli/version` 和 CLI 下载包。

## 9. 回滚和记录

后端由部署脚本处理启动失败回滚。Web 发布前保留旧的
`/opt/chat-codex/web` 和 `/opt/chat-codex/web-codex`，客户端发布保留上一版 APK、
EXE、DMG 和 `version.json`。

每次发布记录发布日期、项目、旧版本、新版本、Git commit、执行命令、测试结果、
线上验证结果、产物 SHA256 和回滚位置。
